{
  "name": "@archlens/realtime",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time communication — Socket.io + SSE + WebRTC",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "socket.io-client": "^4.8.0"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@archlens/config": "workspace:*",
    "@archlens/types": "workspace:*",
    "@types/react": "^18.3.0",
    "typescript": "^5.7.3"
  }
}
