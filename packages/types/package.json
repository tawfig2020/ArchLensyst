{
  "name": "@archlens/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types for ArchLens platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@archlens/config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
