{
  "name": "@archlens/monitoring",
  "version": "1.0.0",
  "private": true,
  "description": "Performance monitoring — Sentry RUM + SpeedCurve + Web Vitals",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@sentry/react": "^8.40.0",
    "@sentry/vite-plugin": "^2.22.0",
    "web-vitals": "^4.2.0"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@archlens/config": "workspace:*",
    "@archlens/types": "workspace:*",
    "@types/react": "^18.3.0",
    "typescript": "^5.7.3"
  }
}
