{
  "name": "@archlens/edge",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers edge caching and routing",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "wrangler": "^3.90.0",
    "typescript": "^5.7.3"
  }
}
